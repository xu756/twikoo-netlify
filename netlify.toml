[[headers]]
  # Define which paths this specific [[headers]] block will cover.
  for = "/"
    [headers.values]
    Access-Control-Allow-Origin = "*.xu756.top"
    Access-Control-Allow-Headers = "Content-Type"
    Access-Control-Allow-Methods = "*"
